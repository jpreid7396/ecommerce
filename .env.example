# Example Firebase config - copy to .env and fill with your project values
# Do NOT commit real secrets. Keep .env in .gitignore.

# Client-side (public) Firebase config - used by frontend
NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-measurement-id

# Server-side / Admin config - used by firebase-admin (server only)
# Option A: point to a service account JSON file on the server
GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/serviceAccountKey.json

# Option B: or set the service account JSON directly (single-line JSON).
# If you use this, ensure it is properly escaped and not committed.
# FIREBASE_SERVICE_ACCOUNT_KEY='{"type":"service_account", "project_id":"...", "private_key_id":"...", "private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n", "client_email":"...", "client_id":"..."}'

# Minimal fallback project id (some admin SDK flows require this)
FIREBASE_ADMIN_PROJECT_ID=your-project-id

# Optional: remote config or other server-only flags
REMOTE_CONFIG_NAMESPACE=default

# Example host for emulator (if used locally)
FIREBASE_EMULATOR_HOST=localhost:8080
